1) Так як $_GET масив, йому можна передавати і двомірні масиви - http://localhost:4000/get_array.php?id[a]=1&id[b]=2&id[c]=10

															[id] => Array
																(
																	[а] => 1
																	[b] => 2
																	[с] => 10
																)
																
2) Котеров каже, що це звязано з безпекою, що не можна передавати в юрл всяку белеберду. Щоб закодувати содержиме - є ф-ція urlencode('Пpивeт, мир!') ; // %D0%9F%Dl%...

	- $url = 'greets.php?пame=' . urlencode($_POST['name');
	header("locatioп : $url");
	exit();


3) Це взагалі дивно. Коли у формі є 2 <select> і 1 з них multipart, то тому який multipart імя треба давати як масиву(name='fst[]') інакше файл обработчик (у якому приймається $_POST) буде бачити тільки 1 вибір (останній).

4) is_uploaded_file - Функция возвращает true, если файл $filename был загружен на сервер. Причем в каче­стве аргумента $filename следует передавать имя временного файла на сервере $_FILES ['myFile']['tmp_name']

5) <input type="file" name="file" /> при передачі на сервер файла, файл поміститься у масив $_FILES у якому буде ще масив file уже в якому будуть всі дані про файл

6) Ф-ція glob - шукає файли, які співпадають з шаблоном, що я дав цій ф-ції. glob("*.txt"); Повертає масив

	- glob('./*') - Тобто шлях тут треба писати як шлях в лінуксі.
