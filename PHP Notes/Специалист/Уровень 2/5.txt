1. На відміну від mysqli у mysql не обовязково брати підключення до MySQL у змінну($link) і тручати змінну у всі ф-ції першим параметром.
	- у mysql $link не обовязковий параметр і вказується останнім. Використовується у випадках, коли на треба підключитись до різних ...
2. Також у mysql не можна вибрати БД при зєднанні, тільки через ф-цію mysql_select_db();

сесуріту для строк - mysqli_real_escape_string($link, trim (strip_tags($data) ) );

mysqli_result::free -- mysqli_free_result — Освобождает память, занятую результатами запроса
	- Нужно всегда очищать память функцией mysqli_free_result(), если результаты запроса больше не нужны.

 mysqli_stmt_init - я не поняв чого у файлі з функціями є 2 види підготовлених запросів. Треба загуглити.

 - password_hash - жосткий хеш. Навіть одинакові букви по різному шифруються
